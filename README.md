# ruls - pure Rust ls command implementation

---

### Note: this is a training project for exercising in Rust and learning std library

---

## Usage:

```
ruls [PATH] [ARGUMENTS]

Current supported arguments:

-a, --all - show all files in directory
```